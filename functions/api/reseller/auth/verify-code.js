import { clean,hash,json } from '../../../_lib/admin-auth.js';import { resellerCookie } from '../../../_lib/reseller-auth.js';
export async function onRequestPost({request,env}){let input;try{input=await request.json()}catch{return json({message:'Dados inválidos.'},400)}const email=clean(input.email,180).toLowerCase(),code=clean(input.code,6);const reseller=await env.DB.prepare("SELECT id FROM resellers WHERE email=? AND status='approved' LIMIT 1").bind(email).first();if(!reseller||!/^\d{6}$/.test(code))return json({message:'Código inválido ou expirado.'},401);const row=await env.DB.prepare('SELECT id FROM reseller_login_codes WHERE email=? AND code_hash=? AND expires_at>?').bind(email,await hash(`${email}:${code}`,env.ADMIN_SESSION_SECRET),new Date().toISOString()).first();if(!row)return json({message:'Código inválido ou expirado.'},401);await env.DB.prepare('DELETE FROM reseller_login_codes WHERE email=?').bind(email).run();const token=`${crypto.randomUUID()}${crypto.randomUUID()}`,now=new Date(),expires=new Date(now.getTime()+28800000);await env.DB.prepare('INSERT INTO reseller_sessions(id,reseller_id,email,token_hash,expires_at,created_at) VALUES(?,?,?,?,?,?)').bind(crypto.randomUUID(),reseller.id,email,await hash(token,env.ADMIN_SESSION_SECRET),expires.toISOString(),now.toISOString()).run();return json({ok:true},200,{'Set-Cookie':resellerCookie(token)})}
